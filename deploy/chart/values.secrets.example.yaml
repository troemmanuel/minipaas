# Copie ce fichier en values.secrets.yaml (ignoré par git, voir .gitignore
# racine, motif *.secrets.yaml) et remplace par de vraies valeurs avant de
# faire helm install/upgrade avec -f deploy/chart/values.secrets.yaml.
# Provisoire : l'étape 6 remplacera ce mécanisme par des SealedSecrets.
postgres:
  user: changeme
  password: changeme

rabbitmq:
  user: changeme
  password: changeme